import { createStore } from 'vuex'
import home from '../assets/pages/home.json'


export default createStore({
  state: {
    homeCards: home,
  },
  mutations: {
  
  },
  actions: {
  },
  modules: {
  }
})
